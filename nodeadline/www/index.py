def get_context(context):
	"""Static 'coming soon' placeholder.

	The page is a complete, self-contained HTML document with nothing dynamic to
	inject, so it is served and cached as-is.
	"""
	return context
